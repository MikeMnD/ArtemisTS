// Module
export class FastMath {

    static PI = Math.PI;
    static SQUARED_PI = Math.PI * Math.PI;
    static HALF_PI = 0.5 * Math.PI;
    static TWO_PI = 2.0 * Math.PI;
    static THREE_PI_HALVES = FastMath.TWO_PI - FastMath.HALF_PI;

}
